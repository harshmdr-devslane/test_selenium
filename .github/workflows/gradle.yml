name: Selenium Java CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest # Using linux machine

    steps:
      - name: Running
        uses: harshmdr-devslane/selenium/.github/workflows/main.yml@master
